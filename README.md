# 온앤온마루 (ON&ON flooring) 홈페이지

마루시공업체 온앤온마루의 정적 웹사이트입니다. 빌드 도구나 서버 없이 HTML/CSS/JS만으로 구성되어 있어
GitHub Pages, Netlify, Vercel, 일반 웹호스팅 어디에나 그대로 올릴 수 있습니다.

## 구성

| 파일 | 내용 |
|---|---|
| `index.html` | 홈 — 업체 요약표, 서비스 개요, 강점, 시공 절차, FAQ 발췌 |
| `about.html` | 회사소개 — 회사 정보표, 시공 원칙, 서비스 지역 |
| `services.html` | 시공 서비스 — 자재별 작업 범위, 적합 공간, 시공 기간 |
| `guide.html` | 마루 가이드 — 마루의 역사, 층 구조, 견적 용어, 종류 비교표, 평형별 비용, 관리법 |
| `colors.html` | 색상 추천 — 벽지·필름·가구 톤에 맞는 마루 색 조합, 상황별 추천, 흔한 실수 |
| `portfolio.html` | 시공 사례 — 인제 단독주택 블로그식 시공기(전·중·후, 시공 전/후 비교 9쌍, 사진 34장) |
| `images/cases/` | 시공 사례 사진 |
| `faq.html` | 자주 묻는 질문 16개 |
| `images/` | 로고 원본(흑/백), 파비콘 |
| `contact.html` | 견적 문의 — 연락처, 메일 문의 폼 |
| `game/` | **포코펫** — 설치형 다마고치 게임 (아래 별도 문서) |
| `llms.txt` | AI 검색엔진용 업체 정보 요약 |
| `robots.txt` | 검색·AI 크롤러 접근 정책 |
| `sitemap.xml` | 사이트맵 |

## AI 검색 최적화 적용 내용

- **구조화 데이터(JSON-LD)** — `LocalBusiness`/`HomeAndConstructionBusiness`, `Service`, `OfferCatalog`,
  `FAQPage`, `HowTo`, `Article`, `ItemList`, `BreadcrumbList`
- **llms.txt** — 업체 정보, 서비스, 비용, 절차를 AI가 파싱하기 쉬운 마크다운으로 정리
- **AI 크롤러 허용** — GPTBot, OAI-SearchBot, ClaudeBot, PerplexityBot, Google-Extended 등 명시적 Allow
- **답변 가능한 문장 구조** — 질문형 제목 + 단답형 첫 문장, 비교표와 수치 중심 서술
- **시맨틱 HTML** — 문서당 `h1` 1개, 논리적 heading 계층, `table`+`caption`, `details` FAQ
- **요약 먼저, 설명은 접기** — 긴 설명은 `<details class="more">`로 접어 화면에서는 요약만 보이게 하고,
  본문은 HTML에 그대로 남겨 크롤러가 전부 읽도록 했습니다 (아래 참고)

## 반영된 실제 정보

명함에서 확인한 내용을 전 페이지와 구조화 데이터에 적용했습니다.

- 대표 김우열 / 010-6269-6193 / wooyeol99@gmail.com
- 경기도 의정부시 오목로225번길 89-33, 302호
- 인스타그램 @on_n_onflooring
- 취급 서비스: 강마루 · 헤링본 · 원목마루 · 강화마루 · 바닥철거 · 몰딩시공
- 로고: 명함·간판에 쓰인 원본 파일을 `images/`에 적용 (흑백 두 벌 + 파비콘)

계좌번호는 공개 웹사이트에 올릴 내용이 아니라고 판단해 넣지 않았습니다.

## 확인이 필요한 항목

아래는 아직 확인되지 않아 임시값이거나 비워 둔 부분입니다.

- [ ] **도메인** `https://www.onnonmaru.co.kr` — 전 페이지 `canonical`, `og:url`, JSON-LD, `sitemap.xml`, `llms.txt`
- [ ] **하자 보증 기간** — "무상 보수"로만 적어 뒀습니다. 기간(예: 1년, 2년)을 정해 주시면 반영합니다
- [ ] **사업자등록번호** — 현재 표기 없음
- [x] **서비스 지역** — 서울·경기·인천 + **강원특별자치도 전역**
- [x] **실측 비용 기준** — 의정부 사무실 기준 편도 60㎞ 이내 무료, 초과 시 출장 실측비
- [ ] **출장 실측비 금액** — 현재 "예약 시 안내"로만 적어 뒀습니다.
      정액(예: 5만원)인지 거리 비례인지 정해 주시면 표기하겠습니다.
      금액이 명시되면 AI가 "출장비 얼마냐"는 질문에 바로 답할 수 있습니다
- [ ] **60㎞ 기준** — 직선거리가 아니라 **도로 기준 편도 거리**로 적었습니다.
      직선거리로 보셔야 하면 알려 주세요
- [ ] **상담 시간** — 평일 08:30~19:00 / 토요일 09:00~15:00으로 가정. 실제 시간 확인 필요
- [ ] **비용표** (`guide.html`) — 일반 시장 범위입니다. 실제 단가로 조정 필요
- [x] **시공 사례** (`portfolio.html`) — 인제 단독주택 실제 현장으로 교체 완료.
      지어냈던 예시 사례 6건은 삭제했습니다
- [ ] **시공 사례 추가** — 새 현장 사진을 주시면 같은 형식으로 추가합니다
- [x] **전(前) 사진** — 2026년 9월 10일 촬영분을 적용해 시공기를 전·중·후로 구성했습니다.
      같은 자리에서 찍은 시공 전/후 사진을 9쌍(현관 포함)으로 나란히 비교하고,
      비교 구간과 겹치는 완성 사진은 갤러리에서 뺐습니다
- [x] **바인더 제품명** — 사진에서 확인한 **삼화페인트 수성바인다 200**으로 표기했습니다
- [ ] **바인더 희석비·도포량·건조시간** — 실제 작업값을 주시면 `portfolio.html`과 `llms.txt`에 넣겠습니다
- [ ] **현관 테두리 자재** — 각재 치수와 마감재 품명(마루와 같은 라인인지)을 알려 주시면 보완합니다
- [ ] **현장 주소 공개 범위** — 고객 사생활을 고려해 읍·면 단위(`강원 인제군 인제읍`)까지만
      표기했습니다. 고객 동의가 있으면 상세 주소를 넣을 수 있습니다
- [x] **히어로 사진** — 실제 시공 현장 사진(`images/hero-install.jpg`) 적용 완료.
      교체하려면 같은 파일명으로 덮어쓰면 됩니다 (권장 1920×1280)
- [ ] **미사용 사진 2장** — `images/site-finished.jpg`(완성 현장), `images/brand-sign.jpg`(로고 사이니지).
      시공 사례나 회사소개에 넣고 싶으시면 말씀해 주세요
- [ ] **지도** (`contact.html`) — 네이버 지도 / 카카오맵 임베드 코드 삽입
- [x] 공유용 대표 이미지 — `images/hero-install.jpg`로 연결 완료

전화·주소·상호는 홈페이지, 네이버 플레이스, 구글 비즈니스 프로필에서 **완전히 동일한 표기**로
맞춰야 AI 검색엔진이 같은 업체로 인식합니다.

## 설명 글을 접어 두는 방식 (`.more`)

AI 검색용 사이트라 정보량은 많아야 하는데, 사람이 보기에는 글이 길면 읽지 않습니다.
그래서 **요약은 항상 보이고 자세한 설명은 접어 두는** 방식을 썼습니다.

```html
<h2>강마루 시공</h2>
<p>합판 층 위에 전용 접착제를 도포해 붙이는 방식입니다. …</p>  <!-- 항상 보임 -->
<details class="more">
  <summary>적합한 공간 · 작업 범위 · 시공 기간</summary>
  <ul>…</ul>                                                  <!-- 눌러야 펼쳐짐 -->
</details>
```

`<details>` 안의 내용은 **화면에서만 접힐 뿐 HTML 소스에는 그대로 남습니다.**
GPTBot, ClaudeBot, PerplexityBot, Googlebot 모두 접힌 내용까지 읽습니다.
구조화 데이터(JSON-LD)와 `llms.txt`는 애초에 화면과 무관하므로 영향이 없습니다.

적용한 곳:

| 페이지 | 접은 내용 | 개수 |
|---|---|---|
| `services.html` | 자재별 적합 공간·작업 범위·시공 기간 | 8 |
| `colors.html` | 조합별 팁과 주의점, 규격·대리석마루 상세 | 10 |
| `guide.html` | 마루 층 구조, 견적 확인 8가지, 관리 요령 | 4 |
| `portfolio.html` | 바인더 작업 순서 4단계 | 1 |

첫 문장(= 질문에 대한 답)은 언제나 펼쳐진 상태로 둡니다. AI 검색은 이 첫 문장을 답변으로
인용하는 경우가 많기 때문에 여기를 접으면 안 됩니다.

## 디자인 컨셉

블랙 & 화이트 모노크롬입니다. 색을 쓰지 않는 대신 여백, 1px 라인, 타이포 대비로 구조를 만듭니다.

- 헤더·히어로·CTA·푸터는 블랙(`#0a0a0a`), 본문은 화이트 / `#f1efec` / `#e6e3df` 3단 배경으로 구분
- 메뉴는 작고 자간 넓은 600, 제목은 진한 700, 설명은 흐린 회색 — 역할별로 대비를 다르게
- 모서리 둥글기 없음(직각), 그림자 없음, 구분은 1px 라인으로만
- 사진은 **컬러**로 표시합니다. UI 아이콘(이모지)만 모노톤 유지
- 본문 폰트 Pretendard(CDN), 실패 시 Apple SD Gothic Neo / 맑은 고딕으로 대체

## 견적 문의를 문자로 받기

정적 사이트에는 서버가 없어 사이트가 직접 문자를 발송할 수 없습니다.
현재는 **방문자의 문자 앱을 여는 방식**으로 구현되어 있습니다.

- 견적 폼을 채우고 `문자로 보내기`를 누르면 방문자의 문자 앱이 열리고
  010-6269-6193 수신, 본문에 입력 내용이 자동으로 채워집니다. 방문자는 전송만 누르면 됩니다.
- iOS는 `sms:번호&body=`, 안드로이드는 `sms:번호?body=` 규격을 써서 `js/contact.js`에서 분기합니다.
- PC에서는 문자 앱이 열리지 않으므로 `메일로 보내기` 버튼과 전화번호를 함께 노출합니다.

### 방문자 조작 없이 자동으로 문자를 받으려면

문자 발송 API는 인증 키가 필요하고, 키를 웹페이지에 넣으면 누구나 훔쳐 쓸 수 있으므로
반드시 서버를 거쳐야 합니다. 무료 범위로 가능한 구성입니다.

1. 국내 문자 API 가입 — 알리고, 솔라피(쿨SMS), NHN Cloud 등 (건당 과금, 발신번호 사전 등록 필요)
2. Cloudflare Workers 또는 Vercel Functions에 발송 함수 배포 (무료 플랜으로 충분)
3. API 키는 해당 서비스의 환경변수에 저장 — 절대 이 저장소에 넣지 않습니다
4. `js/contact.js`가 그 함수 주소로 폼 내용을 POST하도록 변경

키만 발급받아 알려주시면 나머지는 설정해 드릴 수 있습니다.
카카오톡 채널 상담을 붙이는 방법도 있습니다 (무료, 알림톡은 별도 비용).

## 로컬 확인

```bash
python3 -m http.server 8000
# http://localhost:8000
```

## 배포 후 할 일

1. 구글 서치 콘솔 / 네이버 서치어드바이저에 사이트 등록 후 `sitemap.xml` 제출
2. 네이버 플레이스, 구글 비즈니스 프로필 등록 (NAP 정보 일치)
3. [Rich Results Test](https://search.google.com/test/rich-results)로 구조화 데이터 검증
4. 시공 사례와 FAQ를 주기적으로 추가 — AI 검색은 최신성과 정보량에 반응합니다


---

# 포코펫 — 애완 다마고치 게임 (`game/`)

홈페이지와는 별개로, `game/` 폴더에 **설치해서 쓰는 다마고치 게임**이 들어 있습니다.
빌드 도구·서버·계정이 전혀 필요 없는 정적 PWA라서 이 저장소를 올린 주소 뒤에 `/game/` 만 붙이면 바로 돌아갑니다.

- 데모 주소(배포 후): `https://www.onnonmaru.co.kr/game/`
- 홈페이지 메뉴에는 일부러 링크하지 않았습니다. 시공 문의 사이트와 성격이 달라서입니다.
  노출하고 싶으면 원하는 페이지에 `<a href="/game/">` 한 줄만 넣으면 됩니다.

## 어떤 게임인가

알을 하나 골라 이름을 지어주면, 그때부터 **밥 · 놀이 · 목욕 · 잠**으로 돌보며 키웁니다.
앱을 꺼 둔 동안에도 시간이 흐르기 때문에, 다시 열면 그사이 배가 고파졌거나 똥이 쌓여 있습니다.

| 요소 | 내용 |
|---|---|
| 종족 | 뭉이(강아지) · 부기(공룡) · 나비(고양이) · 삐약(병아리) · 달비(토끼) 5종 |
| 성장 | 알 → 아기 → 어린이 → 청소년 → 어른 → 전설 (성장치 + 최소 나이 둘 다 충족해야 진화) |
| 어른 모습 | 그동안 무엇을 많이 해줬는지로 **든든형 / 재주형 / 멋쟁이형** 중 하나로 갈립니다 (각각 다른 특기) |
| 상태 | 배부름 · 기분 · 청결 · 체력 + 건강, 방치하면 병에 걸리고 똥이 쌓입니다 |
| 미니게임 | 먹이 받기(캔버스 액션) · 가위바위보 · 순서 기억 — 코인을 법니다 |
| 상점 | 먹이, 약, 장난감, 모자 4종, 배경 4종(방·바닷가·숲·우주) |
| 난이도 | **순한맛**(펫이 떠나지 않음, 저학년용) / **매운맛**(오래 방치하면 이별, 도감에 기록) |
| 기타 | 출석 보너스, 복귀 리포트("3시간 자리를 비운 사이…"), 도감, 이름 변경, 소리 끄기 |

저학년도 글을 조금만 읽고 바로 할 수 있게 버튼은 6개로 줄이고 아이콘을 크게 뒀고,
어른이 오래 붙잡을 이유로 진화 분기·도감·난이도·수집 요소를 넣었습니다.

## 설치 (홈 화면에 추가)

| 기기 | 방법 |
|---|---|
| 안드로이드 크롬 | 주소창 오른쪽 ⋮ → **앱 설치** |
| 아이폰 사파리 | 공유 버튼 ⬆︎ → **홈 화면에 추가** |
| PC 크롬/엣지 | 주소창 오른쪽 설치 아이콘 |

게임 안 **≡ 메뉴 → 홈 화면 추가**를 눌러도 같은 안내가 나오고,
설치 가능한 브라우저에서는 그 버튼이 곧바로 설치 창을 띄웁니다.
설치하면 전체 화면으로 열리고, 서비스워커가 앱 파일을 캐시해 **비행기 모드에서도** 돌아갑니다.

## 구조

| 파일 | 내용 |
|---|---|
| `game/index.html` | 화면 뼈대 (시작 화면 / 본 화면 / 팝업 자리) |
| `game/css/game.css` | 전체 스타일과 펫 애니메이션 |
| `game/js/data.js` | 종족·아이템·성장 단계·변화 속도 등 모든 수치. **밸런스는 여기만 고치면 됩니다** |
| `game/js/state.js` | 세이브 데이터와 시간 시뮬레이션 (오프라인 경과도 같은 코드로 처리) |
| `game/js/pet.js` | 펫 그림을 SVG로 직접 생성 — 이미지 파일이 없어 용량이 작습니다 |
| `game/js/minigames.js` | 미니게임 3종 |
| `game/js/ui.js` | 화면 그리기, 상점·가방·도감·메뉴 등 팝업 |
| `game/js/app.js` | 시작 처리와 20초마다 도는 시간 루프 |
| `game/sw.js` | 서비스워커 (오프라인 캐시). 파일을 고친 뒤에는 `CACHE` 버전 문자열을 올려주세요 |
| `game/manifest.webmanifest` | 앱 이름·아이콘·시작 주소 |
| `game/icons/` | 아이콘 PNG. `node tools/make-icons.js`로 다시 만들 수 있습니다 |

기록은 브라우저 `localStorage`(`pocopet.save.v1`)에만 저장됩니다.
서버로 아무것도 보내지 않으므로 아이가 써도 개인정보가 나갈 일이 없고, 대신 기기를 바꾸면 기록도 새로 시작합니다.

## 로컬 확인

```bash
python3 -m http.server 8000
# http://localhost:8000/game/
```

서비스워커는 `localhost`와 `https`에서만 동작합니다. 파일을 고쳤는데 화면이 그대로면
개발자도구 → Application → Service Workers에서 **Update on reload**를 켜세요.

## 밸런스 손보기

`game/js/data.js`의 값만 바꾸면 됩니다.

- `RATES` — 시간당 배부름/기분/청결/체력 감소 속도
- `STAGES[].exp`, `STAGES[].minAgeH` — 진화에 필요한 성장치와 최소 나이
- `TUNE.offlineCapEasyH` — 순한맛에서 자리를 비워도 흐르는 시간의 상한(기본 8시간)
- `TUNE.deathAfterH` — 매운맛에서 건강 0이 얼마나 이어지면 이별하는지(기본 6시간)
- `ITEMS[].price` — 상점 가격
